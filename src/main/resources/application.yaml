spring:
  application:
    name: streaming

  datasource:
    url: jdbc:postgresql://localhost:5432/db
    username: root
    password: root
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: always